
It seems there is an issue with the `position` parameter. The error message indicates that `position` is not defined, but it is actually a parameter of the `overlay_text` function. This might be a false positive or an issue with the flake8 configuration.

However, to ensure the code is correct, let's verify the `position` parameter and make sure it is being used correctly. The `position` parameter should be a tuple of two floats representing the x and y coordinates.

Let's recheck the `overlay.py` file to ensure everything is correct.

overlay.py
