# vox-actor — Service-layer dependencies only.
#
# NOTE: torch / torchvision / torchaudio are installed directly in the
#       Dockerfile using the official AMD ROCm index:
#         --index-url https://download.pytorch.org/whl/rocm6.0/
#       Do NOT add them here — doing so risks pip pulling NVIDIA/CPU wheels
#       from PyPI instead of the ROCm-specific index.
#
# NOTE: CosyVoice and its transitive deps (funasr, openai-whisper, etc.) are
#       handled inside the Dockerfile via the upstream requirements.txt clone.

# FastAPI runtime
fastapi
uvicorn[standard]
python-multipart

# Model download / management
modelscope

# Audio I/O (used by app.py)
soundfile
numpy<2.0.0
setuptools<70.0.0
torchcodec

# Logging
loguru
